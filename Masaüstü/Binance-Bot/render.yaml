services:
  - type: web
    name: binance-bot
    env: python
    plan: free
    rootDir: .
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -k eventlet -w 1 app:app
    runtime: python-3.11.9
    # Ortam değişkenlerini Render UI üzerinden ekleyebilirsiniz.